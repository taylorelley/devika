# Frontend Configuration
# Optional: Add additional allowed host for Vite dev server
# If not set, only localhost and 127.0.0.1 are allowed
# Example: VITE_ALLOWED_HOST=devika.example.com
VITE_ALLOWED_HOST=
